import { useLoaderData } from "react-router-dom";

// export const rootLoader = async () => {
//   return { contacts: [{ id: "1" }] };
// };

export const Home = () => {
  // const { contacts } = useLoaderData();
  return <div>Latest observations</div>;
};
